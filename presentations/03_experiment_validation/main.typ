// Experiment Validation — Data Resplit & Full Supervision Scan (Phase 3, detailed)
#import "@preview/touying:0.6.1": *
#import themes.metropolis: *

#show: metropolis-theme.with(
  aspect-ratio: "16-9",
)

#set text(font: ("DejaVu Sans", "Noto Sans SC"), size: 14pt)

// ---------- helpers ----------
#let rec(num) = num + " "  // spacer for pacing (no-op)

// ============================================================
// Slide 1: Title
// ============================================================

#title-slide(
  title: [织物缺陷检测：全实验验证与总结报告],
  subtitle: [Data Resplit · 17 类检测 · 训练侧全杠杆扫描 · 无 NMS 架构探针],
  extra: [
    报告日期 2026-08 · 面向外部读者（自含术语） \
    运行环境：RTX 3060 Laptop 6GB · WSL2 · ultralytics + timm \
    第 3 阶段：划分数次重跑 + 超参探索后的最终结论
  ],
)

// ============================================================
// Slide 2: 任务与探索主线
// ============================================================

#slide[
  == 任务定义与本次探索目标

  检测任务：织物图像上的 17 类缺陷框检测（类别编号 0-16，见数据页）。
  训练数据：1277 张完整图（v2 划分）→ 2,758 个标注框；部分图像含多个缺陷。
  硬件约束：6 GB 显存，1280 训练 batch≤4，本阶段大部分实验 \@640 / batch 16。
  *最终评估约束*：只提交训练权重，禁止任何推理端技巧（TTA / 高分辨率推理 / 集成）。

  #v(0.3em)
  *探索主线*（按顺序，前两阶段已完结）：
  - ① DINOv3 融合：把自监督特征（DINOv3 convnext / vit）接入 YOLO 骨干 → 本报告第 9 页
  - ② 特征提取器与历史改造排查：注意力（003-017）、类别权重、增强（019-031）→ 第 6-7 页、10 页
  - ③ 数据重划 + 干净口径重跑（v2 系列 044-049,057）：消除旧 val 偏置后的对照 → 第 11 页
  - ④ 架构探针（yolov10s / yolo26s 无 NMS）+ yolo26s 超参搜索（050-057）→ 第 12-13 页

  *正式评分口径*（本版修订）：*self-val*（各权重在自身训练划分的 val 上评分）+ *conf=0.25*
  宏召回主基准（best-F1 点参考）。此前两个错误口径全部作废：
  conf→0 的 maxR/peakR 兜底值（FP 堆出的虚高召回）与跨划分验证（v2 val 中 80.9% 的图
  在 v1 训练集里出现 → v1 模型跨集高分全是训练泄漏）。

  *主线结论预览*：① ② ③ ④ 在正式口径下*全部无正收益*（含曾被误报为“唯一候选”的 cos 调度）
  → 见尾页。
]

// ============================================================
// Slide 3: 评测指标详解
// ============================================================

#slide[
  == 评分标准（docs/GOAL.md）与评测实现

  #v(0.1em)
  *竞赛五项评分*：
  - Item1 缺陷检出率 ≥95%（30%）：能否在某个置信度操作点达到全类宏平均召回 ≥95%
  - Item2 过检率≤5% + 误检率≤5%（15%）
  - Item3 小样本学习：1-/3-/5-/10-shot 平均检出率（15%）
  - Item4 推理 ≥30fps \@1024×1024 \@RTX3080（20%）；Item5 创新性与报告（20%）

  #v(0.2em)
  *正式评测口径*（本版修订，`eval_scoring.py` 以同步）：*self-val + conf=0.25* ——
  各权重在*自身训练划分*的 val 上、conf=0.25 推理 → 每类宏平均 P/R（每类等权），
  辅以 best-F1 操作点作参考。
  - *Item1 判定*：宏召回 \@conf=0.25 ≥95% → PASS；否则 FAIL（报告 R\@0.25 与 gap）。

  #v(0.3em)
  *本报告口径修订记录（两轮错误口径，全部作废）*：
  - ① conf≈0 的 *maxR/peakR*（曾报 0.8175/0.8056 “得分/正收益”）：该操作点实测
    P≈0.06–0.08、FP 为 TP 的 25–28 倍（每图 ~40 框 vs GT ~2 框）——堆框买来的虚高召回。
  - ② *跨划分验证*（v1 权重在 v2 val 上评分）：v2 val 350 图中 283 张（80.9%）在 v1
    训练集内 → v1 模型的“0.73–0.81 高分”是训练泄漏（053 跨集 0.8095，self-val 实为 0.5750）。

  *正式口径下所有模型 Item1 均 FAIL*：全局最高 R\@0.25 = 019（0.6213，v1 划分）；
  交付基线 044（yolo11s v2）= 0.5316。没有任何模型接近 95%。

  #v(0.3em)
  *Item2 障碍*：过检/误检需 clean（无缺陷）图作基准，v2 val 350 张全部含缺陷 → 无法直接测（proxy 缺失）。
  *Item3 补测*：044 基准 few-shot 微调 K=1/3/5/10 已按 conf=0.25 口径完成评估（见第 15 页）。

  #v(0.3em)
  *数据的代价*：类别不平衡（最弱 yy 24、最强 wy 327 框），val 单类 17–127 实例；
  单实例 ≈ 0.3–6% 召回，|Δ| < 1% 的结论在单次 run 内不可区分（种子方差，见第 5 页）。

  #v(0.3em)
  *数据的代价*：类别不平衡（最弱 yy 24、最强 wy 327 框），val 单类 17–127 实例；
  单实例 ≈ 0.3–6% 召回，|Δ| < 1% 的结论在单次 run 内不可区分（种子方差，见第 5 页）。
]

// ============================================================
// Slide 4: 数据总览
// ============================================================

#slide[
  == 数据总览：17 类与分布（v2 训练集 1277 图）

  #grid(
    columns: (1fr, 1fr),
    column-gutter: 0.8em,
    [
      #table(
      columns: (auto, auto, auto, auto, auto, auto, auto, auto),
      stroke: 0.4pt,
      align: center,
      inset: 2pt,
      text(size: 9pt)[*\#*], [*类名*], [*框数*], text(size: 9pt)[*占比*],
      text(size: 9pt)[*\#*], [*类名*], [*框数*], text(size: 9pt)[*占比*],
      [0], [lj], [120], [4.4%], [8], [bj], [149], [5.4%],
      [1], [bd], [316], [11.5%], [9], [jt], [87], [3.2%],
      [2], [heidian], [300], [10.9%], [10], [yy], [24], [0.9%],
      [3], [wy], [327], [11.9%], [11], [bmss], [253], [9.2%],
      [4], [zyc], [113], [4.1%], [12], [zy], [192], [7.0%],
      [5], [zmty], [242], [8.8%], [13], [pd], [121], [4.4%],
      [6], [cq], [98], [3.6%], [14], [HD], [108], [3.9%],
      [7], [zj], [118], [4.3%], [15], [cy], [118], [4.3%],
      [16], [jiaodai], [72], [2.6%],
    )
    ],
    [
      #image("class_distribution.png", height: 4.1in)
    ],
  )

  #v(0.3em)
  - 严重长尾：yy(0.9%)、jiaodai(2.6%)、jt(3.2%) 极弱类；bd/wy/heidian/bmss 四大类占比高（柱状图蓝色）。
  - 实例数不是唯一瓶颈：[029] 逆频类别权重 mAP50 由 0.611 降至 0.596（R\@0.25 0.580，无增益）→ 弱类是“小目标难检”而非“样本少”。
  - 每图缺陷密度低（2758/1277 ≈ 2.2 框/图），单实例价值高，漏检代价大。
]

// ============================================================
// Slide 5: 数据划分演进与泄漏
// ============================================================

#slide[
  == 划分演进：旧划分 → v2 新划分（及泄漏证据）

  _旧划分（初赛版本）_：train 1445 / val 181 / test 180（test 未参加训练，val 按 20% 随机）。
  问题：分层天然失衡 — yy 类 val 仅 2 例、HD 9 例；小来源类完全不见；val 太小（单实例≈2.5% 召回）。

  #v(0.2em)
  _泄漏实证_（跨划分验证全面作废）：v2 val 350 图中 *283 张（80.9%）与 v1 训练集重叠*
  —— v1 系模型训练时已见过这些图的像素级内容，跨集高分全是记忆性虚高。
  案例：053（y26s 200ep）在 v2 val 上曾测 0.8095 → self-val（v1 val）实为 *0.5750*；
  004 跨集 0.8918 → self-val 0.6140。跨划分验证在本文档中不再作为任何结论依据。

  #v(0.3em)
  *v2 新划分*（stratified_split.py，按来源×主类全局配额，seed 42）：
  #table(
    columns: (auto, auto, auto),
    stroke: 0.5pt,
    align: center,
    inset: 5pt,
    [*Split*], [*图像数*], [*用途*],
    [train], [*1277*], [训练（所有 v2 重训）],
    [val], [*350*], [评分（干净口径）],
    [test], [*180*], [留验（GT 并入过 045）],
  )

  #v(0.3em)
  *铁律*：评测一律 *self-val*（各权重在自身训练划分的 val 上评分）+ *conf=0.25* 主基准；
  不同划分间分数不可比，跨划分验证仅作泄漏证据，不作结论依据。
]

// ============================================================
// Slide 6: 实验索引（001-019）—— 每个编号对应一次完整训练
// ============================================================

#slide[
  == 实验编号索引 A（001–019：基线与早期改造）

  #set text(size: 8.5pt)

  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.3pt,
    align: center,
    inset: 2pt,
    [*No.*], [*实验内容（模型 \@分辨率）*], [*格式*], [*mAP50*], [*R\@0.25 (self)*],
    [001], [yolov8n 基线], [100ep/16], [0.573], [0.5901],
    [002], [yolov8s 基线], [同], [0.573], [0.5859],
    [003], [yolo11n 基线], [同], [0.580], [0.5803],
    [004], [yolo11s 基线], [640/16/100ep], [0.611], [*0.6140*],
    [005], [yolo26n 基线], [同], [0.537], [0.5097],
    [006], [yolo26s 基线], [同], [0.575], [*0.5660*],
    [007], [YOLO-World-v2s], [同], [0.536], [0.5794],
    [008], [randaug 探索（旧）], [中断], [—], [0.6103],
    [009], [PSA-p3（YOLO 改造）], [同], [0.483], [0.4971],
    [010], [PSA-p4], [同], [0.483], [0.4558],
    [011], [PSA-p345], [同], [0.442], [0.4303],
    [012], [yolov8s-PSA-p5], [同], [0.513], [0.5224],
    [013], [yolov8s-PSA], [同], [0.535], [0.5374],
    [014], [yolo11s-PSA], [同], [0.583], [0.5859],
    [015], [World-v2 + PSA], [同], [0.497], [0.4899],
    [016], [yolo11s-enhanced], [同], [0.519], [0.5041],
    [017], [P2+深度 SE], [同], [0.489], [0.4982],
    [018], [yolo11s \@1280（分辨率杠杆）], [200ep/batch4], [0.517], [0.5630],
    [019], [1280 + 类别权重], [同0.4], [0.525], [*0.6213*],
  )
  \*\* = R\@0.25 = 宏召回 \@conf=0.25（*self-val*：各权重在自身训练划分的 val 上评分，见第 3 页）。
  全部实验 Item1（≥95%）判定 FAIL，数值仅作*同划分内*相对比较。019 为本组最高。
]

// ============================================================
// Slide 7: 实验索引（020–039）
// ============================================================

#slide[
  == 实验编号总览 B（020–039：分辨率 / 弱监督 / DINOv3 融合）

  #set text(size: 8.5pt)

  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.2pt,
    align: center,
    inset: 2pt,
[*No.*], [*实验名称*], [*设置*], [*mAP50*], [*R\@0.25 (self)*],
    [020], [1280 + 强增强], [200ep/4], [0.537], [0.4870],
    [021], [1280 + 冻结 backbone 10], [同], [0.452], [0.5337],
    [022], [yolo11m \@1280], [200ep/2], [0.449], [0.4266],
    [023], [P2 无缩放], [1280/4], [0.066], [0.0368],
    [024], [yolo11s \@640 300ep], [3x 长训], [0.598], [0.6019],
    [025], [DINOv3 融合 v1（浅层 PS）], [640/2], [0.389], [—],
    [026], [DINOv3 full（深层冻结）], [640/4], [0.435], [0.4838],
    [027], [DINOv3 融合 v2], [640/2], [0.420], [0.4379],
    [029], [yolo11s + cls_pw 类别加权], [640/16], [0.596], [0.5800],
    [030], [多尺度训练（旧）], [中断], [—], [0.5422],
    [031], [copy-paste 增强], [640/16], [0.572], [0.5952],
    [032], [weak 集微调（弱数据）], [640/16/50ep], [0.367], [0.5814],
    [033], [weak 集 + 1280], [1280/4], [0.392], [0.5326],
    [034], [1280 过采样（弱数据）], [1280/4], [0.508], [0.5430],
    [035], [DINOv3-convnext 0ep], [640/8], [0.102], [0.0998],
    [036], [DINOv3-vitms], [640/4], [0.071], [0.0486],
    [037], [DINOv3-hybrid], [640/8], [0.079], [0.1278],
    [038], [DINOv3-convnext \@640 50ep], [50/8], [0.223], [0.2429],
    [039], [DINOv3-vitms 50ep], [50/4], [0.219], [0.2420],
  )
  025 无法加载（custom 反序列化失败）未重测；其余旧 maxR/peakR 数值一律作废（口径修订见第 3 页）。
]

// ============================================================
// Slide 8: 实验索引（040–057）
// ============================================================

#slide[
  == 实验编号总览 C（040–057：v2 重跑与超参探索）

  #set text(size: 8.5pt)

  #table(
    columns: (auto, auto, auto, auto, auto, auto),
    stroke: 0.2pt,
    align: center,
    inset: 2pt,
    [*No.*], [*实验*], [*设置*], [*mAP50*], [*R\@0.25 (self)*], [*备注*],
    [040], [DINOv3-hybrid 50ep], [640/8], [0.250], [0.2435], [续训降分],
    [041], [convnext +100ep], [640/8], [0.258], [0.2233], [续训降分],
    [042], [vitms +100ep], [640/4], [0.246], [0.2662], [续训降分],
    [043], [hybrid +100ep], [640/8], [0.288], [0.3050], [续训降分],
    [044], [yolo11s v2 基准], [640/16/100ep], [0.516], [*0.5316*], [全 v2 标准线],
    [045], [v2 + test GT], [同 044], [0.494], [0.5476], [v2 组最高：+14% 数据 +3.0%],
    [046], [v2 + 1280过采样], [1280/4], [0.448], [0.5327], [分辨率无增益],
    [047], [046 无预训练], [1280/4], [0.448], [0.5327], [=046 重复权重（无效变量）],
    [048], [046 暖启动], [1280/4],[0.444], [0.5189], [同负],
    [049], [v2 + 多尺度], [640/16], [—], [0.3574], [-32.8% 负],
    [050], [yolo10s 旧划分], [640/16], [0.565], [0.5354], [v1 组：低于 004],
    [051], [yolo10s v2], [640/16],[0.552], [0.4988], [-6.2% vs 044],
    [052], [yolo26s v2], [640/16], [0.530], [0.4759], [-10.5% vs 044],
    [053], [y26s 200ep], [640/16], [0.586], [0.5750], [v1 组；跨集 0.8095 泄漏作废],
    [054], [y26s + cos], [640/16], [0.564], [0.5625], [v1 组；cos 无增益（-0.6% vs 006）],
    [055], [y26s lr0=0.02], [—], [0.575], [0.5660], [=006 重复权重，无效],
    [056], [y26s 200ep+cos], [640/16], [0.574], [0.5891], [v1 组；长训微正（+4.1%）仍 FAIL],
    [057], [y26s v2 + cos], [640/16],[0.511], [0.4824], [-9.2% vs 044（负信号）],
  )

  #v(0.2em)
  *注*：050–057 中 050/053/054/055/056 为 v1 划分训练（self-val 于 v1 val），
  044/045/046/047/048/049/051/052/057 为 v2 划分——*两组分数不可互比*；
  跨划分曾出现的高分（053 跨集 0.8095 等）为训练泄漏（见第 5 页），作废。
]

// ============================================================
// Slide 9: DINOv3 融合（Ⅰ）
// ============================================================

#slide[
  == DINOv3 融合：三条路线与结果

   *路线设计*（超参查 ultralytics cfg/yaml + scripts/extract_dinov3.py）：
  - *convnext 路线*：DINOv3 convnext_tiny（384ch / stride16,768ch / stride32）+ YOLO 检测头，P4/P5 冻结
  - *vitms 路线*：DINOv3 ViT-S / patch16（vit_base）+ 分类头分离训练
  - *hybrid 路线*：浅层 YOLO(0-4，P3/8 输出) + 深度 DINOv3（P4/P5 冻结）

  #v(0.3em)
  *评分一览（正式口径 R\@0.25，self-val）*：036 单调 0.049 ≪ 037 0.128 ≪ 全套 0.22–0.48，
  远低于同划分基准 004（0.6140）与 044（0.5316）。
  *续训结果（#041-043）*：mAP 涨（0.223→0.258 等）但 R\@0.25 继续跌（0.2429→0.2233 等），
  证明是"过拟合弱类"而非收益延迟。

  #v(0.3em)
  *决定性实验（同弱数据集同 640）*：032（yolo11s 微调）= 0.5814 vs 040（hybrid）= 0.2435 → 差距 ≈ 0.34
  分 —— 融合在弱数据线性不如纯 YOLO。

  #v(0.3em)
  *热力图证据*（scripts/check_heatmap.py）：conv32 特征图对*大目标*聚焦（bmss 误差 0.025），
  对小目标（bd）重心漂移（0.42+）→ 大语义模型在小目标检测上是"优势盲区"。

  #v(0.3em)
  *冻结核验*：040 best vs timm pretrained max|Δ| = 3.9e-3（冻结成立）；042 = 4.9e-2（全微调）→ 思路里
  "冻结深层保语义"假设下无效。非 DINO 特征器对比因网络下载超时而未完成（空缺）。
]

// ============================================================
// Slide 10: 分辨率与弱数据研究（旧口径）
// ============================================================

#slide[
  == 分辨率 / 数据分布杠杆（正式口径）

  *分辨率训练*（018/022 1280 训 200ep）：self-val R\@0.25 = 0.5630 / 0.4266，
  低于同划分基准 004（0.6140）→ 训练侧无收益；旧“1280 推理 +0.06-0.08”属推理侧手段（终版禁用，见 16 页）。

  *弱数据微调*（032/033）：yolo11s 在 0.367 mAP 的弱标注集上微调 50ep → 0.5814，仍低于全量 004（0.6140）。

  *过采样*（034 + 046/048）：034（v1 过采样×3）self-val 0.5430；v2 复刻 046/048 = 0.5327 / 0.5189，
  均 ≤ 044 基线 0.5316 → 过采样无增益。旧口径 0.8703 高分是划痕 val 泄漏镜像，作废。

  #v(0.4em)
  *结论*：训练侧"分辨率 / 弱数据 / 过采样"在正式口径下全部无正收益。
]

// ============================================================
// Slide 11: v2 重排与重训矩阵
// ============================================================

#slide[
  == 数据重排 + v2 重训矩阵（正式口径 R\@0.25：0.5316 基准）

  *重排理由*（第 5 页泄漏证据）：旧 val 偏置可以制造 ±4% 的假增益（034）。
  *重训清单*（全部新 train 1277；self-val \@ v2 val 350 评分）：

  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: center,
    inset: 3pt,
    [*实验*], [*含义*], [*R\@0.25*], [*Δ vs 基*], [*结论*],
    [044 基], [yolo11s 640 100ep], [*0.5316*], [—], [新基准（仍 FAIL）],
    [045], [+14% test GT], [0.5476], [+3.0%], [v2 组最高：数据量增益],
    [046], [\@1280 过采样], [0.5327], [+0.2%], [分辨率/过采样无增益],
    [047], [046 无预训练], [0.5327], [±0], [=046 重复权重，无效变量],
    [048], [046 暖启动], [0.5189], [-2.4%], [同负],
    [049], [640 多尺度 0.5], [0.3574], [-32.8%], [慢 5× 同 ep 负],
  )

  #v(0.3em)
  *Warm-start 管道 bug*（scripts/train.py）：yolo_model 指定 .pt 时 pretrained 字段被忽略
  （旧 034 恰好 yolo_model='018.pt' 所以没踩中）；修后 048 才真正 warm。更改差异见 6-8 页脚注。

  #v(0.3em)
  *结论*：v2 clean 口径下无任何训练侧手段超过基线 —— 045 的 +3.0% 来自 +14% test GT
  数据量而非方法改进。此前的“唯一开延长 = 调度（cos）”已证伪（057 在 v2 上为负，见 14 页）。
]

// ============================================================
// Slide 12: 架构探针：无 NMS（yolo10s / yolo26s）
// ============================================================

#slide[
  == 架构对比：无 NMS 检测头探针

  *动机*：NMS 会在重叠小目标上互相压制 → 无 NMS 架构（one-to-one）是否更有利于召回。
  *注意：仅同划分内可比*（v1 组 / v2 组各自 self-val）：

  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: center,
    inset: 3pt,
    [*架构（v1 组）*], [*头*], [*R\@0.25*], [*Δ vs 004*], [*Δ vs 006*],
    [004 yolo11s], [one-to-many (NMS)], [*0.6140*], [0], [—],
    [006 yolo26s], [end-to-end (无NMS)], [0.5660], [-7.8%], [0],
    [050 yolo10s], [one-to-many + one-to-one], [0.5354], [-12.8%], [-5.4%],
    [054 y26s+cos], [同 006], [0.5625], [-8.4%], [-0.6%],
  )

  #table(
    columns: (auto, auto, auto, auto),
    stroke: 0.4pt,
    align: center,
    inset: 3pt,
    [*架构（v2 组）*], [*R\@0.25*], [*Δ vs 044*], [*结论*],
    [044 yolo11s], [*0.5316*], [0], [基线],
    [051 yolo10s], [0.4988], [-6.2%], [双头难收敛],
    [052 yolo26s], [0.4759], [-10.5%], [无 NMS 无优势],
    [057 y26s+cos], [0.4824], [-9.2%], [cos 亦负],
  )

  #v(0.4em)
  - *yolo10s*：双头结构在小数据（1.4k）收敛不了 → 两划分组均显著落后。
  - *yolo26s*：两组内均低于 yolo11s（无 NMS 声明不成立）；曾在旧口径报的
    “微弱正 +0.3%（006）/ +1.2%（057）”全部来自 conf→0 兜底值或跨划分泄漏，作废。
  - y26s 系列进入超参搜索的动机已失去支撑（见下一张）。
]

// ============================================================
// Slide 13: y26s 超参搜索（旧口径）
// ============================================================

#slide[
  == yolo26s 超参搜索（单变量网格，v1 划分 · self-val）

  #table(
    columns: (auto, auto, auto, auto),
    stroke: 0.4pt,
    align: center,
    inset: 3pt,
    [*实验*], [*改动*], [*R\@0.25*], [*vs 006*],
    [006 基线], [100ep · no-cos · lr≈4.76e-4], [0.5660], [—],
    [053], [epochs 200], [0.5750], [+1.6%],
    [054], [cos_lr = True], [0.5625], [*-0.6%*],
    [056], [200ep + cos], [0.5891], [+4.1%],
    [055], [lr0 = 0.02], [0.5660], [=006 重复],
  )

  #v(0.3em)
  *055 是无效格*（两重证据）：① ultralytics optimizer=auto 打印 "ignoring lr0" 并自行定
  AdamW lr_fit = 0.002×5/(4+nc) = 4.76e-4（nc=17），lr0 参数不生效；② 055 与 006 权重
  state_dict 全等（md5 仅差 ckpt 元数据）→ 确认是同一 checkpoints 的重复 run，剔除。

  #v(0.3em)
  *结论（正式口径）*：cos 无增益（054 = -0.6% vs 006；曾报的“+0.5% 唯一稳定正信号”是
  conf→0 兜底值所致，作废）。长训 053/056 在 v1 组微正（+1.6% / +4.1%），但单种子未复跑、
  且仍低于同划分 004 基线（0.6140）——不进 v2 验证。所有值均在 Item1 FAIL 区（\<95%）。
]

// ============================================================
// Slide 14: 057 v2 验证
// ============================================================

#slide[
  == cos_lr 的 v2 验证：正式口径下证伪

  #table(
    columns: (auto, auto, auto),
    stroke: 0.4pt,
    align: center,
    inset: 4pt,
    [*实验*], [*R\@0.25（v2 self-val）*], [*Δ vs 044*],
    [044 yolo11s 基准], [*0.5316*], [—],
    [052 yolo26s（no cos）], [0.4759], [-10.5%],
    [*057 yolo26s + cos*], [0.4824], [-9.2%],
  )

  #v(0.4em)
  *证伪判定*：057 此前报的“+1.2% 唯一正信号”来自 conf≈0 的 maxR 兜底值（该点 P≈0.075、
  FP 为 TP 的 25 倍——堆框虚高）。正式口径下 057 低于 044 基准 9.2%；cos 对同架构 052
  仅 +1.4%（单种子、噪声区间），无法弥补 y26s 架构本身的缺口 → *cos 调度不再视为任何正信号*。

#v(0.3em)
  *Item1 瓶颈定位*（057 各类召回，v2 val）：
  - *正式阈值 conf=0.25*：难检类极低 —— bd 0.15 / heidian 0.20 / zmty 0.25 / wy 0.32 /
    bmss 0.32 / HD 0.32 / lj 0.26；大目标/易检类达标：jiaodai 1.00 / jt 0.81 / zyc 0.72。
  - *尺寸证据*（val GT 框中位面积占比）：极小类 bd 0.01% / heidian 0.00% / lj 0.02%；
    中小类 zmty 1.8% / bmss 0.2% / wy 0.04%（均有长尾）；*HD 中位 11.1% —— 并非小目标*，
    低召回来自对比度/类内变异。共同点：尺寸小 × 低对比 → 难检，而非单一"小目标"因素。
  - 即使放宽到 conf→0 的*峰值*口径（每类最优 conf，P≈0.05）：bd 0.51 / HD 0.64 /
    heidian 0.66 / zmty 0.68 / bmss 0.72 / wy 0.74 —— *全框算上仍 \<95%*。
  → 达标差距集中在 bd / heidian / zmty / wy / bmss / HD / lj 七类检出能力（与热力图证据一致），
  正常阈值下差距被进一步放大。

  #v(0.3em)
  *落锤*：所有候选（057 cos、053/056 长训）在同划分内均未超过基线，"正收益候选"已不存在，
  无需 seed 复跑 —— *044（0.5316）为交付基线*（同为 FAIL）。
]

// ============================================================
// Slide 15: GOAL.md 五维评分对照（新增）
// ============================================================

#slide[
  == GOAL.md 五维评分对照（当前最优）

  #set text(size: 12pt)

  #table(
    columns: (auto, auto, auto),
    stroke: 0.4pt,
    align: center,
    inset: 3pt,
    [*评分项（权重）*], [*当前最优表现*], [*状态*],
    [Item1 缺陷检出率 ≥95% (30%)], [044 交付基线 R\@0.25 = 0.5316；同组最高 045 = 0.5476（全局最高 019 = 0.6213，v1 组）], [*FAIL*],
    [Item2 过检率≤5% + 误检率≤5% (15%)], [无 clean（无缺陷）图可测], [不可测],
    [Item3 1/3/5/10-shot 平均检出率 (15%)], [044 few-shot 微调：K1 0.5344 / K3 0.5350 / K5 0.5317 / K10 0.5348，平均 0.5340（3 seeds，std ≤0.009）], [*FAIL*（= 044 基线 0.5316，微调零增益）],
    [Item4 ≥30fps \@1024² \@RTX3080 (20%)], [044：45.5fps 实测 → ~113.7fps 估], [*PASS*],
    [Item5 创新性与技术报告 (20%)], [本报告 + 三融合路线探索], [—],
  )

  #v(0.3em)
  *Item4 实测*（本机 RTX3060 6GB，×2.5 折算 3080）：
  057 640² 57.5fps（~143.7）/ 1024² 45.5fps（~113.7）/ 1280² 33.5fps（~83.8）→ 1024² 全 PASS；
  044、052、054 同 PASS（~113–128fps \@1024² 折算）。

  #v(0.3em)
  *本阶段节目*：Item1 是唯一硬指标 FAIL —— 主因 7 个难检类（bd/heidian/zmty/wy/bmss/HD/lj，尺寸×对比度，见 14 页）；
  Item2 需补 clean 图评估集。few-shot（Item3）已完成：14% 图像微调不改变检出率（K1–K10 全平）。
  全套分数均以 *self-val + conf=0.25* 口径（第 3 页）。
]

// ============================================================
// Slide 16: 推理侧参考（原 15）
// ============================================================

#slide[
  == 推理侧手段（仅参考——终版排除；下值均为旧口径 peakR，已作废）

  #table(
    columns: (auto, auto, auto, auto),
    stroke: 0.4pt,
    align: center,
    inset: 3pt,
    [*手段*], [*操作*], [*旧评分 (v2 peakR)*], [*结论*],
    [TTA（推理增强）], [044 + val augment], [0.8461], [旧口径唯一“正”，口径作废],
    [1280 推理], [044 权重 \@1280], [0.6109], [跨分辨率崩],
    [集成], [022 系拼接], [—], [被 1280 拖累],
  )

  #v(0.3em)
  *说明*：TTA +4.0% 与 1280 推理均是在 conf→0 兜底口径下测得；按正式口径需重测才有意义，
  但两者属推理端技巧，终局约束（仅交付权重）下不可用 → 不再重测，仅保留历史证据。

  #v(0.3em)
  按终局约束（仅交付权重）以上行全不可用；仅作 evidence 与对照。
]

// ============================================================
// Slide 16: 全杠杆汇总
// ============================================================

#slide[
  == 训练侧全杠杆扫描汇总（正式口径 R\@0.25 · self-val）

  #table(
    columns: (auto, auto, auto, auto),
    stroke: 0.4pt,
    align: center,
    inset: 3pt,
    [*杠杆*], [*代表实验*], [*R\@0.25*], [*结局*],
    [分辨率训练], [046/048 \@1280（v2）], [0.533/0.519], [≤ 基线 044 0.5316],
    [数据倍增], [045 +test GT（v2）], [0.548], [+3.0%：数据量、非方法],
    [多尺度训练], [049（v2）], [0.357], [-32.8%],
    [epoch 长度], [053/056（v1）], [0.575/0.589], [v1 组微正，仍 < 004 0.6140],
    [模型容量], [022 11m\@1280（v1）], [0.427], [-19.4% vs 004],
    [学习率], [055（无效格）], [= 006], [lr0 不生效（auto-AdamW）],
    [调度], [054（v1）/ 057（v2）], [0.5625/0.4824], [cos 无增益 / 负],
    [类别权重], [029（v1）], [0.580], [无增益],
    [注意力], [009–017], [0.43–0.60], [负 / 平台],
    [DINOv3], [025–043], [0.05–0.48], [负],
  )

  #v(0.2em)
  *总结*：正式口径下*所有*训练侧杠杆均无正收益（唯一“增益”045 来自 +14% 数据量而非方法）。
  1.2k 数据下模型超参已饱和 —— 下一步真正的杠杆是标注质量（漏标清洗）与数据量，而不是配方。
]

// ============================================================
// Slide 17: 结论
// ============================================================

#slide[
  == 总结与交付

  #v(0.3em)
  *结论*（正式口径：self-val + conf=0.25；Item1 全 FAIL）：
  1. DINOv3 融合（3 路）全负（R\@0.25 0.05–0.48），热力图证实“只在大目标有效”，不可逆。
  2. 注意力 / 类别权重 / 过采样 / 多尺度 / 1280 训练：全部负或平台（0.43–0.60，均 ≤ 同组基线）。
  3. 无 NMS 架构无优势：y26s（v2 组 -10.5%）与 y10s（-6.2%）均落后 yolo11s。
  4. *无任何训练侧正信号*。曾被报为“唯一正信号”的 yolo26s + cos（057：旧报 0.8175 / +1.2%）
     正式口径下为负（0.4824，-9.2% vs 044）—— 假象来源：conf≈0 兜底值（P≈0.075、
     FP/TP≈25）+ 跨划分训练泄漏（v2 val 80.9% 重叠 v1 训练集）。
  5. 推理端 TTA 等旧口径参考值（0.8461）全局最大，但终局约束禁止使用，且旧口径已作废。

  #v(0.3em)
  *交付物*：
  - `outputs/final_submission_weights.pt` ← 044 yolo11s v2 best（R\@0.25 = 0.5316，唯一交付）

  #v(0.3em)
  *待办（外部建议）*：① ~~Item3 few-shot 收尾~~（已完成：K1–K10 全平 0.532–0.535，与基线无差，
  结论已入第 15 页）；② 数据质量清洗作下一轮主力（漏检框统计脚本已备）；
  ③ 显式 optimizer 重测 lr 维度（仅学术）；
  ④ 上报文档化（本 deck 含两轮口径修订记录）。
]
