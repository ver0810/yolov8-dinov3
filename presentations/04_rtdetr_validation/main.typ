// 织物缺陷检测：数据勘误与 RT-DETR 验证报告（Phase 4 修订版）
// 大纲审定版：说明内容序号分点在前面；每页一个主题；表格统一、数值右对齐
#import "@preview/touying:0.6.1": *
#import themes.metropolis: *

#show: metropolis-theme.with(
  aspect-ratio: "16-9",
)

#set text(font: ("DejaVu Sans", "Noto Sans SC"), size: 15pt)

// ============================================================
// 1. 封面
// ============================================================
#title-slide(
  title: [织物缺陷检测：数据勘误与 RT-DETR 验证报告],
  subtitle: [旧口径勘误 · 三数据体系严格自评 · Transformer 路线验证],
  extra: [
    2026-08 修订版（Phase 4）· 全部数值与仓库脚本可复现 \
    数据集体系：V1 / V2 / V3（严格 self-val，禁跨集比较）
  ],
)

// ============================================================
// 2. 说明：报告目标与正式口径
// ============================================================
#slide[
  == 说明一：报告目标与正式口径

  1. *报告对象*：17 类织物缺陷检测（yolo11s/RT-DETR 训练侧探索），交付训练权重，
     禁用推理端手段（TTA / 高分辨率推理 / 集成）。
  2. *正式口径*：self-val（自集验证）＋ conf\@0.25 ＋ IoU≥0.5 ＋ 17 类宏平均；
     推理分辨率 1280（640 训练模型仅按 640 推理）。
  3. *数据体系*：V1 / V2 / V3 三套划分互不混用，各体系内严格自评；
     跨体系数值（如 V1 的 0.7565 vs V3 的 0.6038）直接比较即为错误。
  4. *数值纪律*：全部正式数值与 `scripts/eval_scoring.py`（仓库内）一致；
     已用 004 / 076 / 097 三个锚点模型复算验证，与历史记录逐位吻合（见附录）。
]

// ============================================================
// 3. 说明：数值评级规则
// ============================================================
#slide[
  == 说明二：数值评级规则（每表来源列与页脚注）

  5. *A 级（磁盘可核）*：数值存于仓库文件（`report_retest_selfval_conf025.csv` /
     `outputs/eval_scoring/*/scoring_report.txt`）或本次复算输出，可逐行核对。
  6. *S 级（评测输出记录）*：权重已清理，仅存当时 `eval_scoring.py` 输出；
     其 maxR 与磁盘报告一致（如 096 的 0.8971）。
  7. *B 级（会话记录）*：V3 体系全部实验（079-093）——v3 数据集、权重、评测报告文件
     均已被环境清理，仅存当时评测输出记录；*仅供内部方向判断*，禁止与 A/S 级排序比较，
     正式引用前需重建 V3 环境复核。
  8. *对比完整性*：每组表格只列同一体系内的实验；跨体系只作趋势说明，不作排序。
  9. *F1 定义*：F1\@0.25 = 2·R·P/(R+P)（同一 conf 处计算），全表统一。
]

// ============================================================
// 4. 旧报告勘误
// ============================================================
#slide[
  == 旧报告数据勘误（01/02/03 版）

  #table(
    columns: (auto, auto, auto),
    stroke: 0.4pt,
    align: (left, left, left),
    inset: 4pt,
    [*旧表述*], [*问题*], [*正确值（本报告口径）*],
    [02：018/022 宏召回 0.8836 / 0.8898，"分辨率杠杆 +0.06-0.08"], [conf→0 兜底值（P≈0.02，堆框虚高）], [018 = *0.5630*；022 = 0.4266 —— 训练分辨率无正收益],
    [02/03：057 "唯一正信号"（旧报 0.8175 / +1.2%）], [兜底值 + v2 跨集泄漏], [057 = *0.4824*（-9.2% vs 044 基线）],
    [03：053 跨集 0.8095 / 004 跨集 0.8918], [v2 val 与 v1 train 重叠 80.9%（泄漏）], [053 = 0.5750；004 = 0.6140（self-val）],
    [早前审计：080 曾报 0.6593], [诊断脚本（r25 类）重复计数口径], [080 = 0.6038（正式评分）],
    [03 注意力实验表 009-017 数值混排], [v1 / v2 划分分数并存"], [009 = 0.4971（v1）；044 = 0.5316（v2）——分列，不可互比],
    [03 结论"训练侧所有杠杆均无正收益"], [该结论截止 057，未包含 RT-DETR 路线], [RT-DETR-L\@1280 = 0.7565，*+13.5pp*，结论被推翻],
    [03 第 10 页 "1280 推理 +0.06-0.08 / TTA 0.8461"], [均为推理端手段（终局禁用）], [保留为禁用证据；不参与交付排名],
  )

  #v(0.2em)
  #text(size: 9pt, style: "italic")[注：以上"问题值"均为旧报告真实输出，非笔误——由口径与泄漏导致；本表仅说明差异与正确值。]
]

// ============================================================
// 5. 数据体系与实验索引
// ============================================================
#slide[
  == 数据体系与实验索引

  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, center, center, center, center),
    inset: 4pt,
    [*体系*], [*训练图数*], [*val 图数 / GT*], [*划分方式*], [*本报告包含实验*],
    [V1], [1445], [181 / 377], [原始划分], [001-019（部分）, 053, 056, 076, 078, 095, 096, 097],
    [V2], [1277], [350 / 678], [类分层重划分], [044-049, 051, 052, 057],
    [V3], [1448], [180 / 428], [8:1:1 图级 + 类分层], [079-093（B 级记录）],
  )

  #v(0.4em)
  *实验编号索引*：
  #table(
    columns: (auto, auto),
    stroke: 0.3pt,
    align: (left, left),
    inset: 3pt,
    [*体系*], [*编号*],
    [V1], [004/008/019（yolo11s 基线）、053/056（yolo26s）、076/078（Kaggle）、095/096/097（RT-DETR）、095_inplace],
    [V2], [044（基线）、045-048（数据/预训练）、049（多尺度）、051/052（yolo10s/yolo26s）、057（cos）],
    [V3], [079-089（Aug 扫描）、091（P2）、092（EQLv2）、093（Seesaw）],
  )

  #v(0.2em)
  #text(size: 9pt, style: "italic")[注：V3 val 每类 GT ≥8（bd 72 / heidian 60），样本量约为 V1 val 的 1.6-1.9 倍；不同体系 val 难度不同，指标水平不具可比性。]
]

// ============================================================
// 6. V1 体系完整对比
// ============================================================
#slide[
  == V1 体系完整对比（self-val：181 图 / 377 GT）

  #table(
    columns: (auto, auto, auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, left, right, right, right, right, center),
    inset: 3pt,
    [*实验*], [*模型 / 配置*], [*R\@0.25*], [*P\@0.25*], [*F1\@0.25*], [*maxR*], [*来源*],
    [004], [yolo11s \@640（基线）], [0.6140], [0.5453], [0.5776], [0.8197], [A],
    [019], [yolo11s + cls_w \@1280], [0.6213], [0.6239], [0.6226], [0.8149], [A],
    [008], [yolo11s + randaug], [0.6103], [0.5661], [0.5874], [0.8466], [A],
    [053], [yolo26s 200ep], [0.5750], [0.6383], [0.6050], [0.7826], [A],
    [056], [yolo26s 200ep + cos], [0.5891], [0.6076], [0.5982], [0.7872], [A],
    [076], [yolo11s Kaggle \@1280], [0.6353], [0.4320], [0.5143], [0.8426], [A],
    [078], [yolo11s + 增强池 \@1280], [0.6217], [0.4185], [0.5003], [0.8504], [S],
    [095_inplace], [yolo11s + 原位小池 \@1280], [0.5279], [0.4307], [0.4744], [0.8979], [S],
    [095], [RT-DETR-L \@640], [0.7070], [0.4437], [0.5453], [0.8304], [S],
    [096], [RT-DETR-L \@1280], [*0.7565*], [0.4029], [0.5258], [0.8971], [S],
    [097], [RT-DETR-L \@1280（显式 AdamW）], [0.7137], [0.3939], [0.5076], [0.8591], [A],
  )

  #v(0.2em)
  #text(size: 9pt, style: "italic")[注：等级见"说明二"；096/097 复算数值见附录（096 权重已清理，仅剩评测记录与 maxR 磁盘佐证）。]
]

// ============================================================
// 7. V1 结论
// ============================================================
#slide[
  == V1 体系结论

  1. *RT-DETR-L\@1280 全链最强*：R\@0.25 = 0.7565，超 YOLO 最优（019 = 0.6213）*+13.5pp*；
     Transformer（去 NMS 的 DETR 解码器）是本项目首个明确正收益架构。
  2. *增强池无增益*：076（0.6353）高于 019，但 078（+合成/放大池 0.6217）反而低于 076
     —— 池不平衡，收益被稀释；095_inplace（0.5279）说明原位小池对 YOLO 本身有限。
  3. *优化器陷阱*：ultralytics `optimizer=auto` 按 `lr_fit = 0.002×5/(4+nc)` 自动定 lr
     （nc=17 → 0.000476）并*忽略用户 lr0*；097 显式锁定 1e-4 反而 -4.3pp（0.7565 → 0.7137）。
  4. *无 NMS 架构在 V2 不成立*（见下页），但在 V1 系 RT-DETR 成立——架构收益与环境相关，
     不能跨体系套用。
]

// ============================================================
// 8. V2 体系对比
// ============================================================
#slide[
  == V2 体系全重跑（self-val：350 图 / 678 GT）

  #table(
    columns: (auto, auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, left, right, right, right, center),
    inset: 3pt,
    [*实验*], [*配置*], [*R\@0.25*], [*P\@0.25*], [*结论*], [*来源*],
    [044], [yolo11s v2（基线）], [0.5316], [0.5355], [V2 标准线], [A],
    [045], [v2 + test GT 并入], [0.5476], [0.5602], [*唯一正收益*：+14% 数据量], [S],
    [046], [v2 + 1280 过采样], [0.5327], [0.5732], [分辨率无增益], [S],
    [047], [046 无预训练], [0.5327], [0.5732], [=046（无效变量）], [S],
    [048], [046 暖启动], [0.5189], [0.5218], [同负], [S],
    [049], [v2 + 多尺度], [0.3574], [0.6187], [*严重负收益*（-17.4pp）], [A],
    [051], [yolo10s v2], [0.4988], [0.6866], [双头架构难收敛], [A],
    [052], [yolo26s v2], [0.4759], [0.6775], [无 NMS 无优势], [A],
    [057], [yolo26s v2 + cos], [0.4824], [0.6155], [cos 仍负（-9.2%）], [A],
  )

  #v(0.2em)
  结论：数据量（045）是 V2 内唯一正向；多尺度 / 过采样 / 无 NMS 架构全部 ≤ 基线。
]

// ============================================================
// 9. V3 体系全扫描（B 级）
// ============================================================
#slide[
  == V3 体系全扫描（B 级记录 · 仅供内部方向）

  *V3 val：180 图 / 428 GT；下表为当时 eval_scoring 输出，资产（数据/权重/报告）已清理。*

  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, left, right, right, right),
    inset: 2.5pt,
    [*实验*], [*配置（V3 self-val / 1280 / conf\@0.25）*], [*R\@0.25*], [*P\@0.25*], [*maxR*],
    [079], [V3-A：原图 + wavelet + contrast], [0.5841], [0.4180], [0.8081],
    [080], [V3-B：原图 + small 原位池], [*0.6038*], [0.3167], [0.8700],
    [081], [small 2x（倍率减半）], [0.5379], [0.3163], [0.8340],
    [082], [class gain 关闭], [0.4371], [0.4490], [0.8232],
    [083], [cls 权重关闭], [0.4293], [0.3723], [0.8318],
    [084], [area 权重关闭], [0.3775], [0.4176], [0.8406],
    [085], [gain = 1.5], [0.4634], [0.3501], [0.8372],
    [086], [V3-B + contrast 池], [0.5385], [0.2819], [0.8196],
    [087], [box 7.5 → 10], [0.5242], [0.3004], [0.8213],
    [088], [yolo11n \@640], [0.4056], [0.5421], [0.7719],
    [089], [mixup + cutmix], [0.4708], [0.2958], [0.8496],
    [091], [P2 头（4 头）], [0.3982], [0.2651], [0.8194],
    [092], [EQLv2（长尾）], [0.3230], [0.5450], [0.8250],
    [093], [Seesaw（长尾）], [0.4353], [0.3944], [0.7951],
  )

  #v(0.2em)
  方向结论：080（small 原位池）内部最优；082-085（增益开关）确认加分项本就有限；
  086（+contrast）反而伤 bd；088\@640 崩塌证明 V3 必须 1280；091（P2）/092/093（长尾）全负。
]

// ============================================================
// 10. 速度与优化器
// ============================================================
#slide[
  == 速度（Item4）与优化器实测

  #table(
    columns: (auto, auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, right, right, right, right, center),
    inset: 4pt,
    [*模型*], [*\@640*], [*\@1024*], [*\@1280*], [*\@3080 折算\@1024*], [*Item4（≥30fps）*],
    [004 yolo11s], [56.1], [51.2], [35.3], [128.0], [PASS],
    [076 yolo11s（1280 训练）], [61.0], [47.8], [33.8], [119.5], [PASS],
    [095 RT-DETR-L \@640], [21.3], [13.1], [9.6], [32.9], [PASS（边缘）],
    [096 RT-DETR-L \@1280], [19.5], [11.9], [9.8], [29.7], [*FAIL（-0.3）*],
    [097 RT-DETR-L \@1280], [21.0], [12.2], [9.0], [30.4], [PASS（边缘）],
  )

  #v(0.2em)
  *说明*：
  1. 数值为 RTX 3060 实测定格；\@3080 = ×2.5 TFLOPS 折算（近似），不同帧率单位 fps。
  2. yolo11s 全程宽裕（119+fps）；RT-DETR-L 三 run 全部紧贴 30fps 红线——
     *Item4 与精度（096 = 0.7565）存在冲突*，需目标机实测后取舍。
  3. 优化器：`auto` 实际 lr = 0.002×5/(4+17) = *0.000476*（忽略 lr0）；
     后续 RT-DETR 建议显式 `AdamW + lr0≈4.8e-4`，避免"改 lr 无效"的假象。
]

// ============================================================
// 11a. 数据集改进研究（一）：097 最好点逐类 mAP
// ============================================================
#slide[
  == 数据集改进研究（一）：097 \@最好点逐类 mAP

  *评估*：RT-DETR-L（097）在 V1 val（181 图 / 377 GT）\@1280 全曲线评估；
  最好点 = 宏平均 F1 最大处，conf=0.548。全局：mAP50 = *0.6363*，mAP50-95 = 0.4328，
  best-F1 点 R=0.6494 / P=0.6581 / F1=0.6406（vs conf\@0.25 的 R=0.7159 / P=0.3925）。

  #table(
    columns: (auto, auto, auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, right, right, right, right, right, right),
    inset: 2.5pt,
    text(size: 8.5pt)[*类*], [*n*], [*AP50*], [*mAP\@.5:.95*], [*bestF1*], [*R\@best*], [*P\@best*],
    [lj], [12], [0.412], [0.302], [0.521], [0.500], [0.544],
    [bd], [44], [0.290], [0.071], [0.383], [0.318], [0.481],
    [heidian], [39], [0.268], [0.117], [0.411], [0.359], [0.481],
    [wy], [39], [*0.162*], [0.071], [0.328], [0.256], [0.454],
    [zyc], [11], [0.955], [0.815], [0.909], [0.909], [0.909],
    [zmty], [37], [0.290], [0.120], [0.360], [0.297], [0.456],
    [cq], [17], [0.605], [0.357], [0.645], [0.588], [0.714],
    [zj], [18], [0.878], [0.811], [0.882], [0.833], [0.937],
    [bj], [22], [0.626], [0.325], [0.732], [0.682], [0.789],
    [jt], [10], [0.826], [0.607], [0.857], [0.900], [0.818],
    [yy], [2], [0.995], [0.845], [0.996], [1.000], [0.993],
    [bmss], [44], [0.349], [0.161], [0.441], [0.386], [0.514],
    [zy], [27], [0.702], [0.429], [0.720], [0.667], [0.782],
    [pd], [20], [0.886], [0.701], [0.865], [0.800], [0.941],
    [HD], [9], [0.850], [0.365], [0.842], [0.888], [0.800],
    [cy], [16], [0.818], [0.494], [0.786], [0.687], [0.917],
    [jiaodai], [10], [0.905], [0.766], [0.952], [1.000], [0.909],
  )

  #v(0.2em)
  弱类排序（AP50 升序）：wy 0.162 < heidian 0.268 < bd 0.290 ≈ zmty 0.290 < bmss 0.349 < lj 0.412；
  强类（AP50 ≥ 0.80）：zyc / yy / zj / jt / pd / HD / cy / jiaodai。
]

// ============================================================
// 11b. 数据集改进研究（二）：GT 缺陷尺寸分布与性能关联
// ============================================================
#slide[
  == 数据集改进研究（二）：GT 缺陷尺寸分布

  *V1 val 全部 377 GT 框尺寸统计（原图 2048 基准；S \<32px / M 32-96px / L \>96px）*：

  #table(
    columns: (auto, auto, auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, right, right, right, right, right, right),
    inset: 2.5pt,
    text(size: 8.5pt)[*类*], [*n*], [*中位边长(px)*], [*S%*], [*M%*], [*L%*], [*AP50（097）*],
    [lj], [12], [111], [33], [17], [50], [0.412],
    [bd], [44], [15], [*100*], [0], [0], [0.290],
    [heidian], [39], [12], [*97*], [3], [0], [0.268],
    [wy], [39], [57], [26], [36], [38], [0.162],
    [zyc], [11], [348], [0], [0], [100], [0.955],
    [zmty], [37], [160], [11], [14], [76], [0.290],
    [cq], [17], [659], [0], [0], [100], [0.605],
    [zj], [18], [981], [0], [0], [100], [0.878],
    [bj], [22], [534], [0], [0], [100], [0.626],
    [jt], [10], [479], [0], [0], [100], [0.826],
    [yy], [2], [594], [0], [0], [100], [0.995],
    [bmss], [44], [76], [18], [34], [48], [0.349],
    [zy], [27], [540], [0], [0], [100], [0.702],
    [pd], [20], [755], [0], [0], [100], [0.886],
    [HD], [9], [576], [0], [0], [100], [0.850],
    [cy], [16], [508], [0], [0], [100], [0.818],
    [jiaodai], [10], [130], [0], [20], [80], [0.905],
  )

  #v(0.2em)
  全局：S = 108（28.6%）、M = 39、L = 230；中位边长 201px。
  *尺寸-性能强关联*：中位边长 \<80px 的类（bd / heidian / wy / zmty / bmss / lj）AP50 全部 ≤0.41；
  中位 >300px 的类 AP50 全部 ≥0.60。小目标是数据集的第一短板。
]

// ============================================================
// 11c. 数据集改进研究（三）：改进方向与预期
// ============================================================
#slide[
  == 数据集改进研究（三）：改进方向与预期

  1. *小目标欠采样是根因*：377 GT 中 28.6%（108 个）为 \<32px 小目标，且集中在 bd（100% S）/
     heidian（97% S）/ wy（26% S）三类；1280 训练时这些缺陷仅 7-16px——训练-推理尺度失配
     （与 V3 阶段诊断一致）。
  2. *wy 是隐藏短板*：此前只盯 bd/heidian；本表显示 wy（39 实例、26% S、弱纹理）AP50 = *0.162*
     全场最低，且 maxR 仅 0.487（漏检多）——应纳入小目标/难例挖掘的第一梯队。
  3. *数据改进动作*（训练侧，符合只交权重约束）：
     - ① 小目标原位池扩展到 wy/bmss（V3-B 已验证 bd/heidian 增益，扩类需重验）；
     - ② 尺寸分层过采样：S 类 ≥2×、M 类 1.5×（简单加权，不改变标注）；
     - ③ 弱纹理类（wy / bmss / zmty）对比度+小波增强（086 在 YOLO 为负，RT-DETR 未验证，需重试）；
     - ④ 难例挖掘补漏检框（wy maxR 0.487 / bmss 0.773 为最高收益区）。
  4. *不再投入*：大目标类（zyc / yy / zj / jt / pd / HD / cy / jiaodai）AP50 ≥0.80 已饱和。
  5. *验证体系*：数据集改进必须在 V3 体系（bd 72 / heidian 60 GT，弱类方差更小）验证，
     避免 V1 val 弱类 39-44 样本的噪声；同时新增 wy / bmss 的 V3 逐类基线。
]

// ============================================================
// 11d. 数据集改进研究（四）：极小缺陷的原图像素与 1280 像素
// ============================================================
#slide[
  == 数据集改进研究（四）：极小缺陷像素尺寸（2048 → 1280）

  *GT 框边长（px）；1280 列 = 2048 × 0.625（等比例 letterbox）*：

  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, right, right, right, right),
    inset: 2.5pt,
    text(size: 8.5pt)[*类*], [*n*], [*原图 2048 中位（min-max）*], [*1280 推理中位（min-max）*], [*判定*],
    [bd（白点）], [44], [15（6-32）], [*9.4（3.7-19.8）*], [*超小*],
    [heidian（黑点）], [39], [12（8-36）], [*7.4（4.7-22.5）*], [*超小*],
    [wy（污印）], [39], [57（11-686）], [35.5（6.6-428.5）], [跨尺度],
    [bmss（表面损伤）], [44], [76（17-1330）], [47.6（10.3-831.2）], [跨尺度],
    [lj（垃圾）], [12], [111（8-747）], [69.6（5.0-466.9）], [跨尺度],
    [zmty（脏面条印）], [37], [160（25-1242）], [99.8（15.3-776.0）], [中大],
    [jiaodai（胶带）], [10], [130（59-146）], [81.0（36.8-90.9）], [中大],
    [HD（横档）], [9], [576（399-789）], [360（249-493）], [*大目标*（非小缺陷）],
  )

  #v(0.2em)
  *关键事实*：
  1. *bd / heidian 是超小目标*：1280 推理下中位仅 9.4 / 7.4px——1280 输入经 32 倍下采样后
     每个特征图单元格 = 40px，*目标比一个 cell 还小*，只可能在 P3/P4 高分辨率特征上残留信号，
     检测器天然弱（对应 AP50 0.29 / 0.27）。
  2. *HD 不是小缺陷*（中位 576px，100% 大目标，AP50 0.85）——"hd" 需与 heidian 区分，
     极小缺陷类是 *bd / heidian*。
  3. *跨尺度类（wy / bmss / lj）*：同一类内从 6px 到 1300px 都有 → 需尺度分桶增强，
     而非整体缩放。
]

// ============================================================
// 11e. 数据集改进研究（五）：patch（原位小目标池）效果
// ============================================================
#slide[
  == 数据集改进研究（五）：patch（原位小目标池）效果

  *V3 体系（B 级，yolo11s \@1280）*：
  #table(
    columns: (auto, auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, left, right, right, right, right),
    inset: 3pt,
    [*run*], [*训练数据*], [*R\@0.25*], [*maxR*], [*ΔR\@0.25*], [*ΔmaxR*],
    [079], [原图 + wavelet + contrast], [0.5841], [0.8081], [—], [—],
    [080], [079 + *small 原位池*（131 图 ×4 光度变体）], [*0.6038*], [*0.8700*], [*+2.0pp*], [*+6.2pp*],
    [081], [080 但 small 池倍率 4x→2x], [0.5379], [0.8340], [-6.6pp], [-3.6pp],
  )

  *V1 体系逐类对照（bd / heidian）*：
  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, left, right, right, right),
    inset: 3pt,
    [*run*], [*数据/架构*], [*bd R\@0.25*], [*heidian R\@0.25*], [*bd AP50*],
    [076], [yolo11s \@1280（无 patch）], [0.205], [0.154], [—],
    [063], [yolo11s + bdhd *合成池* \@1280], [*0.4773*], [*0.3333*], [—],
    [097], [RT-DETR-L \@1280（无 patch）], [0.500], [0.410], [0.290 / 0.268],
  )

  #v(0.2em)
  *效果结论*：
  1. *原位池有效*：V3 上 080 宏观 +2.0pp、maxR +6.2pp——小尺度正样本直接提升召回上限；
  2. *池构成敏感*：081（倍率减半）-6.6pp → 原位尺度保持（不放大、不变换）是收益来源；
  3. *合成池 vs 原位池*：063 合成池对 bd/hei 逐类增益大（0.205→0.4773 / 0.154→0.3333），
     但宏观受弱类挤占（078 < 076）；两者方向一致，可组合；
  4. *RT-DETR 无池基线*：097 的 bd/hei 仍是最弱档（AP50 0.29 / 0.27）→
     *原位池 × RT-DETR × V3 体系是下一步最高优先级组合*。
]

// ============================================================
// 11f. 数据集改进研究（六）：097 推理样图 vs GT（检出案例）
// ============================================================
#slide[
  == 数据集改进研究（六）：097 推理样图 vs GT —— 检出案例

  条件：RT-DETR-L（097）\@1280、conf=0.548（最好点）。每图左 = GT 标注，右 = 097 预测（青色虚线，含 conf）；同一裁剪区域。

  #grid(
    columns: (1fr, 1fr, 1fr),
    column-gutter: 0.8em,
    [#image("examples/tp_bd_pair.jpg", width: 100%) \ *bd 检出*（LZW\_20250418\_1845）],
    [#image("examples/tp_heidian_pair.jpg", width: 100%) \ *heidian 检出*（1021\_\_0921）],
    [#image("examples/tp_big_pair.jpg", width: 100%) \ *大目标类检出*（100\_DHW\_20250103before）],
  )

  #v(0.3em)
  *观察*：小目标（bd / heidian）在最好点下可检出（对应 bd R\@0.25=0.500、hei 0.410），
  但预测框与 GT 对齐度低、置信度分散；大目标类框对齐良好。
]

// ============================================================
// 11g. 数据集改进研究（七）：097 推理样图 vs GT —— 失败案例
// ============================================================
#slide[
  == 数据集改进研究（七）：097 推理样图 vs GT —— 漏检 / 误检

  同条件：\@1280、conf=0.548。每图左 = GT 标注（橙色 = 漏检），右 = 097 预测；同一裁剪区域。

  #grid(
    columns: (1fr, 1fr, 1fr),
    column-gutter: 0.8em,
    [#image("examples/fn_bd_pair.jpg", width: 100%) \ *bd 漏检*（0\_DHW\_20250103）],
    [#image("examples/fn_heidian_pair.jpg", width: 100%) \ *heidian 漏检*（113\_\_0921）],
    [#image("examples/fp_bd_pair.jpg", width: 100%) \ *误检 FP*（0\_DHW\_20250103）],
  )

  #v(0.3em)
  *观察*：漏检集中于极小 bd / heidian（1280 下 4-20px，弱于特征图 cell 40px）；
  误检多为低置信同类噪声框。→ 印证数据集改进研究（五）：需小目标原位池 + 难例挖掘，
  并在 RT-DETR × V3 体系重验（见 11c/11e）。
]

// ============================================================
// 11h. 数据集改进研究（八）：097 混淆矩阵
// ============================================================
#slide[
  == 数据集改进研究（八）：097 混淆矩阵（conf=0.548）

  #align(center)[
    #image("examples/confusion_matrix_097_0548.png", width: 62%)
  ]

  全局：TP = 192、FN（漏检）= 185、FP（误检）= 105。
  *关键事实*：
  1. *几乎零类间混淆*：17 类对角全正、无跨类格（GT 要么正确检出要么漏检）——
     RT-DETR 的类判别能力远强于 yolo11s（079 曾记录 22 个错类，heidian→wy 等）；
  2. *失败模式 = 漏检*：FN 集中于小目标——bd 31/44（70%）、heidian 26/39（67%）、
     wy 29/39（74%）、bmss 29/44（66%），落入背景列（与 11g 样图一致）；
  3. *FP 105*：误检框分散于小/中类，多为低置信同类噪声（对应 11g 误检图）；
  4. *数据改进启示*：瓶颈是*小目标检出*而非*类别判别* → 强化 11c/11e 的
     小目标原位池 + 难例挖掘方向，类别混淆无需处理。
]

// ============================================================
// 12a. 架构融合研究（一）：P2 高分辨率层方案与计算约束
// ============================================================
#slide[
  == 架构融合研究（一）：P2 高分辨率层 —— 可行性与计算约束

  *动机*：RT-DETR 混淆矩阵显示失败模式 = 小目标漏检（FN 185：bd 31/44、hei 26/39、
  wy 29/39、bmss 29/44）；1280 下 bd/hei 仅 7-15px，P3(stride 8) 特征已不足。

  *设计*：RT-DETR backbone 的 P2(stride 4) 特征（HGStem 输出）已存在但 head 未用；
  最小侵入 yaml 接入：upsample X3 → concat P2 → RepC3 → decoder 输入 4 尺度（P2,P3,P4,P5）。

  *可行性验证（本报告写作时实测）*：
  1. `RTDETRDecoder` 的 `nl = len(ch)` + `input_proj` 按通道自动扩展——*原生支持 4 尺度*；
  2. 前向 640 / 1280 均通过，输出 6 解码层 × 300 query × (box4 + cls17)；
  3. 训练冒烟（coco8 \@640，1 epoch）loss 正常反传；参数 32.0M → 34.8M。

  *计算约束（关键发现）*：
  #table(
    columns: (auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, right, right, right),
    inset: 3pt,
    [*变体*], [*GFLOPs \@640*], [*相对原版*], [*Item4 影响*],
    [RT-DETR-L（原版）], [~26], [1.0×], [基准 29.7fps 估],
    [RT-DETR-L + P2（全接入）], [*219*], [*8.5×*], [*不可行*（估 \<5fps）],
  )
  P2 特征图在 1280 输入 = 320×320，token/卷积量爆炸（新增 97% 计算）→ *P2 全接入否决*。
]

// ============================================================
// 12b. 架构融合研究（二）：P2 修正方案与实验矩阵
// ============================================================
#slide[
  == 架构融合研究（二）：P2 修正方案与实验矩阵

  *方案 A（推荐）：P2 辅助监督头* —— 训练侧在 P2 上挂轻量检测头做 aux loss，
  推理端只用 decoder（P3-P5），*零推理开销*（Item4 不变）；P2 梯度监督主网小目标表征。
  风险：091（yolo11s + P2 head）历史为负，但 RT-DETR 解码器全局建模语义不同，需重验。

  *方案 B：P2 轻量接入 decoder*（1×1 proj、无 RepC3）——CCFF 跨尺度 token 仍 ~3×，
  仅 RT-DETR-R18 级可试（速度余量）。

  *方案 C：multi_scale 训练*（零架构改动，RT-DETR 未试；YOLO 049 曾负）。

  #table(
    columns: (auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, left, center, center, left),
    inset: 3pt,
    [*方案*], [*实现*], [*Item4*], [*预期*], [*风险*],
    [A P2 aux 头], [yaml + loss 补丁], [不变（推理无 P2）], [bd/hei R 升 5-10pp], [091 历史负信号],
    [B P2 轻接入], [yaml（R18 级）], [R18 保 30fps 待测], [bd/hei 直接可见 P2], [计算仍高、工程复杂],
    [C multi_scale], [仅训练参数], [不变], [小目标尺度多样性], [049 负信号（YOLO）],
  )

  *决策点*：A（保 Item4，训练侧）与 B（R18 精度优先）需取舍；建议先 A 后 B。
  数据侧与 small 原位池（11e）可叠加。下一步：实现方案 A 的 yaml + aux loss 补丁并本地冒烟。
]

// ============================================================
// 11. 结论与交付建议
// ============================================================
#slide[
  == 结论与交付建议

  #table(
    columns: (auto, auto, auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, left, right, right, center, left),
    inset: 4pt,
    [*场景*], [*模型*], [*R\@0.25*], [*速度（估）*], [*Item4*], [*评价*],
    [V1 精度优先], [096 RT-DETR-L \@1280], [0.7565], [29.7fps], [边缘], [最强召回；交付前必测目标机],
    [V1 速度优先], [095 RT-DETR-L \@640], [0.7070], [32.9fps], [边缘 PASS], [降 5pp 换余量；仍需实测],
    [V1 对照], [019 yolo11s + cls_w], [0.6213], [119.5fps], [PASS], [YOLO 路线最优（速度安全）],
    [V3 重建], [080 配置 yolo11s \@1280], [0.6038\*], [—], [—], [B 级；需重建数据后复核],
  )

  *待办*：① V3 重建后以同 script 复核 080/096；② RT-DETR-R18\@1280（速度 × 精度）；
  ③ 交付前在目标机（3080）实测 Item4；④ `scripts/r25_manual.py` 已作为独立审计匹配器入库。
]

// ============================================================
// 12. 附录：数据来源与复现验证
// ============================================================
#slide[
  == 附录：数据来源与口径复现验证

  *复现验证（本报告写作时，用仓库 `scripts/eval_scoring.py` 对幸存权重重跑）*：
  #table(
    columns: (auto, auto, auto, auto),
    stroke: 0.4pt,
    align: (left, right, right, center),
    inset: 3pt,
    [*模型*], [*历史记录*], [*本次复算*], [*一致性*],
    [004 yolo11s \@640], [0.6140 / 0.5453 / 0.5776], [0.6140 / 0.5453 / 0.5776], [*逐位一致*],
    [076 yolo11s \@1280], [0.6353 / 0.4320 / 0.5143], [0.6354 / 0.4321 / 0.5144], [一致（±0.0001）],
    [097 RT-DETR-L \@1280], [0.7137 / 0.3939 / 0.5076], [0.7159 / 0.3925 / 0.5070], [一致（±0.002）],
  )

  *来源清单*：
  1. 001-057 R/P/maxR：`outputs/report_retest_selfval_conf025.csv`，`final_selfval_delivery.csv`（A）
  2. 076/078/095/096/097 逐类 maxR 与 best_F1：`outputs/eval_scoring/*/scoring_report.{txt,csv}`（A/S）
  3. 076/097 本次复算：`outputs/eval_scoring/{076,097}_reeval_conf025/`（A）
  4. 速度：`outputs/eval_scoring/{004,095,096,097}*/fps_benchmark.json` + reeval 报告（A）
  5. V3（079-093）：当时 eval_scoring 会话输出（B；原资产已清理）
  6. 审计工具：`scripts/r25_manual.py`（conf≥0.25 + IoU0.5 + 17 类宏平均独立实现）

  NOTE[注：另以手写匹配器与 ultralytics `val(conf=0.25)` 交叉验证，两者与 eval_scoring 存在
  4-6pp 的匹配实现差异（TP 分配策略不同）——以 eval_scoring 为唯一正式口径。]
]

// ============================================================
// 13. 结语
// ============================================================
#slide[
  == 结语

  1. *最强模型*：RT-DETR-L \@1280（096，V1 R\@0.25 = 0.7565）——但 Item4 处于边缘（29.7fps 估）。
  2. *可靠路线*：yolo11s（019/076 系列，0.62-0.64 且速度无忧）。
  3. *最重要的方法论修正*：训练侧"尺度对齐 + 原位池 + 架构变更"值得继续；
     背景抑制（070/071）、长尾加权（092/093）、P2（091）方向已关闭。
  4. *数据治理*：历史全部数值可复现（eval_scoring 在仓库）；V3 重建时保留数据 + 权重 + 报告
     三者于仓库内，避免 B 级黑箱再次出现。
]
