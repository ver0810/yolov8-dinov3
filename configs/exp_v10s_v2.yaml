exp_name: "051_yolo10s_v2"
mode: yolo_family
# yolov10s on the v2 stratified split — clean comparison vs 044 (yolo11s v2baseline, 0.8056).
# Same recipe as 044: COCO init, 640, batch16, 100ep.
yolo_model: yolov10s.pt

data_config: configs/own_dataset_v2.yaml
output_dir: outputs/runs

img_size: 640
batch_size: 16
epochs: 100
lr: 0.01
patience: 100