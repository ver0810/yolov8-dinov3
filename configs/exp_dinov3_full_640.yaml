exp_name: "026_dinov3_full"
mode: full
# model yaml resolved by scripts/train.py --mode full → yolov8-dinov3.yaml
# DINOv3 weights: auto-downloaded via timm from HuggingFace

data_config: configs/own_dataset.yaml
output_dir: outputs/runs

img_size: 640
batch_size: 4
epochs: 100
lr: 0.001
patience: 100
