exp_name: "025_yolo11s_dinov3_fusion"
mode: yolo_family
yolo_model: third_party/ultralytics/ultralytics/cfg/models/v8/yolov8-dinov3-fusion.yaml

# yolo11s backbone weights are loaded internally by YOLO11sDINOv3Fusion
# DINOv3 weights: auto-downloaded via timm from HuggingFace

data_config: configs/own_dataset.yaml
output_dir: outputs/runs

# Matches 004 baseline: 640, batch=16, 100 epochs
img_size: 640
batch_size: 2
epochs: 100
lr: 0.001
patience: 100
