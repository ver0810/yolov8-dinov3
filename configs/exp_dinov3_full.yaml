exp_name: A1_dinov3_full_frozen
mode: full
# model yaml resolved by scripts/train.py --mode full
# TODO(dinov3): after real weights, load inside DINOv3Backbone

data_config: configs/tianchi.yaml
output_dir: outputs/runs

img_size: 640
batch_size: 8
epochs: 100
lr: 0.001
patience: 50
