exp_name: A3_dinov3_hybrid
mode: hybrid
# TODO(dinov3-hybrid): implement CSP early + DINOv3 deep in yolov8-dinov3-hybrid.yaml

data_config: configs/tianchi.yaml
output_dir: outputs/runs

img_size: 640
batch_size: 8
epochs: 100
lr: 0.001
patience: 50
